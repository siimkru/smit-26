# Set this to an organization-approved OpenAI model before enabling the agent.
OPENAI_MODEL=example-approved-model
OPENAI_TEMPERATURE=0.2
# Keep the real key out of this repository. Export it in the shell or use a local .env file.
OPENAI_API_KEY=
